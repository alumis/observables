{
  "name": "@alumis/observables",
  "version": "2.0.6",
  "description": "An efficient, single-threaded observable library with support for computed, arrays and sets.",
  "scripts": {
    "bump": "git add -A & git commit -am 'No message' && npm version patch && npm publish && git push --follow-tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alumis/observables.git"
  },
  "author": "Emil Müller",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/alumis/observables/issues"
  }
}
